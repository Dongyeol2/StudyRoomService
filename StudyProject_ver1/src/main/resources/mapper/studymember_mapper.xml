<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="studymember">
	<insert id="addstudymember" parameterType="studymembervo">
		insert into studymember
		(studyno, userid, status)
		values (#{studyno}, #{userid}, #{status})
	</insert>
	<update id="">
		update studymember set attend = 1 where userid='user07'
	</update>
	
 </mapper>










